# Architecture Decision Records

## 1. 커스텀 HTML 렌더링 시 보안(XSS) 및 관리자 권한 격리 문제
* Context : 운영자가 직접 업로드하거나 AI가 생성한 외부 HTML 파일에는 악의적인 스크립트가 포함되어 있을 가능성이 존재합니다. 이 HTML이 관리자 대시보드와 동일한 환경에서 렌더링될 경우, 쿠키 및 세션을 탈취하는 XSS 공격에 취약해집니다.

* Decision : 배포용 폼 뷰어(/f/[id]) 페이지에서 iframe을 도입하고 sandbox="allow-scripts allow-forms" 속성을 부여하여 렌더링 환경을 격리했습니다.

* Consequence : allow-same-origin 속성을 배제하여, 악성 스크립트가 실행되더라도 관리자의 인증 정보나 브라우저 스토리지에 접근할 수 없도록 원천 차단했습니다.


## 2. 대시보드 실시간 동기화 시 서버 과부하 방지
* Context : 대규모 캠페인 시 트래픽이 몰려서 데이터가 짧은 시간에 급증할 경우, 관리자 대시보드에서 매번 데이터를 fetch하면 서버와 클라이언트에 과부하가 발생할 수 있습니다.

* Decision : 폴링 방식 대신 이벤트 기반의 Supabase Realtime(웹소켓)을 도입, 클라이언트 갱신 로직에 Debouncing을 적용했습니다.

* Consequence : 초당 수백 건의 이벤트가 발생하더라도, 타이머를 통해 이벤트를 병합하여 대시보드 새로고침 API 호출을 2초에 1번으로 강제 제한함으로써 성능과 안정성을 모두 확보했습니다.


## 3. 동일인 중복 신청에 따른 데이터 무결성 및 전환율 왜곡 방지
* Context : 공개 신청 폼의 특성상 방문자가 실수로 새로고침을 하거나 여러 번 제출할 경우, DB에 쓰레기 데이터가 쌓이고 캠페인 전환율이 실제보다 과장되는 문제가 발생할 수 있습니다.

* Decision : api/leads/route.ts에서 폼 데이터를 DB에 저장하기 전, 해당 캠페인 ID와 전화번호(Phone)의 조합을 조회하여 이미 등록된 신청자인지 서버 단에서 교차 검증하는 중복 방지 로직을 구현했습니다.

* Consequence : 한 명의 방문자는 하나의 캠페인에 한 번만 신청할 수 있게 되어, 정확하고 오염되지 않은 고유 데이터만 쌓이게 했습니다. 이를 통해 보다 정확한 채널별 전환율 통계를 제공할 수 있습니다.